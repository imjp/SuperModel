<h1>Editing <%= @user.name %></h1>

<%= render "form" %>   

<%= link_to 'Show', @user %> |
<%= link_to 'Back', :back %> |
<strong><%= link_to 'Destroy', @user, confirm: 'Are you sure?', :method => :delete %></strong>